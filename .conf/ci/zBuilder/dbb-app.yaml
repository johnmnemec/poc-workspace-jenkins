---
#
# Licensed materials - Property of IBM
# 5655-AC5 Copyright IBM Corp. 2024
# All rights reserved
# US Government users restricted rights  -  Use, duplication or
# disclosure restricted by GSA ADP schedule contract with IBM Corp.
###
# NJL copy of zbuilder 2.0.3 beta samples. Updated for poc-app  (see poc-app)
#
application:
  name: poc-app
  tasks:
    - task: Cobol
      variables:
        # dependency search path
        - name: dependencySearchPath
          value: search:${WORKSPACE}/?path=${APP_DIR_NAME}/copybook/*.cpy
          
        # Need to build sub-module epsnbrvl.cbl first during cobol builds
        - name: fileBuildRank
          value: 1
          forFiles: "**/*sub*"

        # Skip creating a load module for these programs as they will be 
        # statically linked to other programs
        - name: skipLinkEdit
          value: true
          forFiles: 
            - "**/cobol/nop.cbl"
            

    - task: LinkEdit
      variables: 
        # Set flag for link cards with CICS load modules
        - name: hasCICSModules
          value: true
          forFiles:
            - "**/link/nop.lnk"               